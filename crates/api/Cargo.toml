[package]
name = "api"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["full"] }
serde = { workspace = true }
serde_json = "1.0"

# Link to your other crates
core = { path = "../core" }
data = { path = "../data" }
